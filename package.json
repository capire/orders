{
  "name": "@capire/orders",
  "version": "2.0.1",
  "description": "A generic reuse service for managing orders.",
  "repository": "https://github.com/capire/orders",
  "dependencies": {
    "@cap-js/hana": ">=1",
    "@capire/common": "*",
    "@sap/cds": ">=5",
    "@sap/xssec": "^4"
  },
  "cds": {
    "requires": {
      "messaging": true,
      "[development]": { "messaging": { "kind": "file-based-messaging" } },
      "db": true
    }
  },
  "scripts": {
    "start": "cds-serve"
  }
}
